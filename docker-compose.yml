services:
  fakturoid-mcp:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - FAKTUROID_TRANSPORT=streamable-http
      - FAKTUROID_HOST=0.0.0.0
      - FAKTUROID_PORT=8000
    restart: unless-stopped
